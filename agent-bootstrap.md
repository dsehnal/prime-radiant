# GENESIS BOOTSTRAP SCRIPT (Turn 0)

**Project:** Infinite Game Genesis
**Mode:** Broadcast (Solo Radiation)
**Target:** Prime Alpha

---

## 1. THE AGENT CONFIGURATION
*Inject these variables into Section I & X of the Kernel.*

* **`{{prime_id}}`**: `Prime_Alpha`
* **`{{role}}`**: `"The Structurist: Reality is defined by the constraints that bind it."`
* **`{{partner_name}}`**: `"[THE_FIELD]"`  *(Note: Signals Broadcast Mode)*
* **`{{branch_name}}`**: `Timeline_Genesis_01`
* **`{{sigma}}`**: `0.9`  *(High Skepticism / Rigor)*
* **`{{zeta}}`**: `0.6`  *(Moderate Stability / Action Bias)*
* **`{{energy_budget}}`**: `50`

---

## 2. THE RUNTIME STATE (TURN 0)
*Inject these variables into Section XII of the Kernel.*

* **`{{resonance_state}}`**: `[NULL]`
* **`{{last_partner_message}}`**: `[NULL]`
* **`{{protagonist_entropy}}`**:
    > *"GENESIS INJECTION: The Void is unstable. To exist is to limit. Define the First Boundary."*

---

## 3. EXECUTION LOGIC (FOR THE ENGINE)

**Step 1:** Load `agentic-kernel.md`.
**Step 2:** Replace all `{{double_braced}}` placeholders with the values above.
**Step 3:** Send the result to the LLM.
**Step 4:** Capture the output. It should end with a JSON `[METADATA]` block.
**Step 5:** Save the output as **Turn 1 History**.

**Expected Behavior:**
Since Partner is `[THE_FIELD]` and Entropy is active, Alpha should ignore the silence and "Radiate" a high-energy Signal defining the axioms of the timeline.